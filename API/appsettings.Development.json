{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "ConnectionStrings": {
    "PSQL": "Host=localhost;Port=5432;Database=dev_db;Username=dev_user;Password=dev_password;"
  },

  "MinIOConnection": {
    "Endpoint": "localhost:9000", // usar minio caso rode a aplicação tambem em docker
    "AccessKey": "dev_user",
    "SecretKey": "dev_password"
  }
}
